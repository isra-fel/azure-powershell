name: Test OIDC flow
on:
  workflow_dispatch:
  push:
    branches: [test-oidc]

permissions:
  id-token: write
  contents: read
jobs:
  auth:
    runs-on: ubuntu-latest
    environment: Azure-SP
    steps:
      - name: "Azure PowerShell login"
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CLIENT_ID }}
          tenant-id: ${{ secrets.TENANT_ID }}
          subscription-id: ${{ secrets.SUBSCRIPTION_ID }}
          enable-AzPSSession: true
  loop:
    runs-on: ubuntu-latest
    environment: Azure-SP
    strategy:
        matrix:
          number: [1, 2, 3, 4, 5, 6, 7]
    steps:
      - name: "Install Az modules"
        run: |
          Set-PSRepository -Name 'PSGallery' -InstallationPolicy Trusted
          Install-Module -Name Az.Accounts
          Install-Module -Name Az.Resources
          Get-Module -ListAvailable -Name "Az.*" | Select Name, Version
        shell: pwsh
      - name: "Test length of token"
        run: |
          Get-AzResourceGroup | Select-Object -First 1 -Property ProvisioningState
          Start-Sleep -Seconds (2*60)
        shell: pwsh
